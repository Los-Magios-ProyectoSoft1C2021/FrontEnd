//Funcion de busqueda por lista.
/*
function searchFunction() {
    var input, filter, li, i, a;

    input = document.getElementById('destino');//id de input
    filter = input.nodeValue.toLocaleLowerCase();
    ul = document.getElementById('wrapper');
    li = ul.getElementById('li'); //elemento de la lista

    for (i = 0; i < li.length; i++) {
        a = li[i].getElementByTagName('a')[0];
        if (a.innerHTML.toUpperCase().indexOf(filter) > -1) {
            li[i].style.display = "";
        }

        else {
            li[i].style.display = 'none';
        }
    }

}
*/